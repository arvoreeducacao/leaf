## What changes

<!-- What someone sees or gets that they did not before. Not the diff — the effect. -->

## Why

<!-- The problem this solves. Link the issue if there is one. -->

## How to check it

<!-- The commands you ran, the screens you opened. `pnpm typecheck` is the minimum. -->

- [ ] `pnpm typecheck` passes
- [ ] user-facing strings added to both `messages/pt-BR.json` and `messages/en-US.json`
- [ ] commits are signed off (`git commit -s`)
